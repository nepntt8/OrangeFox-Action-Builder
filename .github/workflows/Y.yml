name: OrangeFox - Build (Non-Vanilla)

# Credits to:
# https://github.com/TeamWin
# https://gitlab.com/OrangeFox
# https://github.com/azwhikaru for Recovery Builder Template
# And all Contributors in every repositories I used

on:
  workflow_dispatch:
    inputs:
      MANIFEST_BRANCH:
        description: 'OrangeFox Manifest Branch'
        required: true
        default: '12.1'
        type: choice
        options:
        - 12.1
        - 11.0
      DEVICE_TREE:
        description: 'OrangeFox Device Tree'
        required: true
        default: 'https://github.com/cd-Crypton/custom_recovery_tree_realme_nashc'
      DEVICE_TREE_BRANCH:
        description: 'OrangeFox Device Tree Branch'
        required: true
        default: 'android-12.1'
      DEVICE_PATH:
        description: 'Specify your Device Path'
        required: true
        default: 'device/realme/nashc'
      DEVICE_NAME:
        description: 'Specify your Device Codename'
        required: true
        default: 'nashc'
      BUILD_TARGET:
        description: 'Specify your Build Target'
        required: true
        default: 'recovery'
        type: choice
        options:
        - boot
        - recovery
        - vendorboot
      AVB_DISABLE:
        description: 'Full AVB Disable (vbmeta --disable-verity --disable-verification)'
        required: false
        default: 'true'
        type: boolean
      NON_VANILLA:
        description: 'Non-Vanilla Build (All Features)'
        required: false
        default: 'true'
        type: boolean

jobs:
  build:
    name: Build OFR Non-Vanilla by ${{ github.actor }}
    runs-on: ubuntu-22.04
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
              
    - name: Clean-up
      uses: rokibhasagar/slimhub_actions@main

    - name: Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 24
      
    - name: Build Environment
      run: |
        sudo apt install aria2 -y
        git clone https://gitlab.com/OrangeFox/misc/scripts.git -b master
        cd scripts
        sudo bash setup/android_build_env.sh
      
    - name: Set-up Manifest
      if: inputs.MANIFEST_BRANCH == '11.0' || inputs.MANIFEST_BRANCH == '12.1'
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/OrangeFox
        cd ${GITHUB_WORKSPACE}/OrangeFox
        git config --global user.name "${{ github.actor }}"
        git config --global user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
        git clone https://gitlab.com/OrangeFox/sync.git -b master
        cd sync
        ./orangefox_sync.sh --branch ${{ inputs.MANIFEST_BRANCH }} --path ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}

    - name: Clone Device Tree
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}
        git clone ${{ inputs.DEVICE_TREE }} -b ${{ inputs.DEVICE_TREE_BRANCH }} ./${{ inputs.DEVICE_PATH }}
        cd ${{ inputs.DEVICE_PATH }}
        echo "COMMIT_ID=$(git rev-parse HEAD)" >> $GITHUB_ENV

    - name: Setup Non-Vanilla + AVB Flags
      if: inputs.NON_VANILLA == 'true'
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}/${{ inputs.DEVICE_PATH }}
        
        cat > vendorsetup.sh << 'EOF'
#!/bin/bash
        
# ========================================
# FULL NON-VANILLA + AVB DISABLE
# ========================================
export OF_PATCH_AVB20=1
export OF_SUPPORT_VBMETA_AVB2_PATCHING=1
export OF_DISABLE_AVBMETA=1
export OF_VBMETA_DISABLE_FLAGS=1
export OF_FLASH_VBMETA_SYSTEM=1
export OF_USE_MAGISKBOOT=1
export OF_USE_MAGISKBOOT_FOR_ALL_PATCHES=1

# AVB/Essential Bypass
export OF_DONT_PATCH_ENCRYPTED_DEVICES=1
export OF_NO_RECRYPT=1
export FOX_OWE_DISABLED=1
export OF_RECOVERY_LP_MODE=1
export OF_DISABLE_DM_VERITY_INDICATOR=1

# NON-VANILLA FEATURES
export FOX_USE_BASH_SHELL=1
export FOX_ASH_IS_BASH=1
export FOX_USE_NANO_EDITOR=1
export FOX_USE_TWRP_RECOVERY_IMAGE_BUILDER=1
export OF_SUPPORT_FBE_OS=1
export OF_NO_MIUI_PATCH=0

# Extra Tools & Features
export FOX_USE_TAR_BINARY=1
export FOX_USE_SED_BINARY=1
export FOX_USE_ZIP_BINARY=1
export FOX_USE_DIGEST_CACHE=1
export FOX_DISABLE_APP_MANAGER=1
export OF_NO_TWRP_FOLDER=1

# GSI + Dynamic Support
export OF_SUPPORT_GSI=1
export FOX_SUPPORT_GSI=1
export FOX_DYNAMIC_PARTITION=1
export OF_USE_GSI_PARTITION=1

# Custom Messages
export FOX_WARNING_MESSAGE_BOOT="Use volume keys to select package(s) and swipe to flash"
export FOX_WARNING_MESSAGE_WIPE="/sdcard/"
EOF
        chmod +x vendorsetup.sh
        echo "✅ Non-Vanilla flags enabled ($(grep -c '^export' vendorsetup.sh) total)"

    - name: Building OrangeFox ${{ inputs.NON_VANILLA == 'true' && '[Non-Vanilla]' || '' }}
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}
        export ALLOW_MISSING_DEPENDENCIES=true
        export LC_ALL="C"
        export FOX_BUILD_DEVICE=${{ inputs.DEVICE_NAME }}
        
        set +e
        source build/envsetup.sh
        if [ -f "${{ inputs.DEVICE_PATH }}/vendorsetup.sh" ]; then
          source ${{ inputs.DEVICE_PATH }}/vendorsetup.sh
        fi
        set -e
        lunch twrp_${{ inputs.DEVICE_NAME }}-eng && make clean && mka adbd ${{ inputs.BUILD_TARGET }}image

    - name: Set Release Properties
      run: |
        echo "BUILD_DATE=$(TZ=Asia/Manila date +%Y%m%d)" >> $GITHUB_ENV
        echo "NON_VANILLA=${{ inputs.NON_VANILLA }}" >> $GITHUB_ENV
        echo "AVB_DISABLE=${{ inputs.AVB_DISABLE }}" >> $GITHUB_ENV

    - name: Upload to Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.img
          OrangeFox/fox_${{ inputs.MANIFEST_BRANCH }}/out/target/product/${{ inputs.DEVICE_NAME }}/OrangeFox*.zip
        name: "OrangeFox [NV:${{ inputs.NON_VANILLA }}|AVB:${{ inputs.AVB_DISABLE }}] ${{ inputs.DEVICE_NAME }} (${{ inputs.BUILD_TARGET }}) // ${{ env.BUILD_DATE }}"
        tag_name: ${{ github.run_id }}
        body: |
          ## 🦊 OrangeFox Recovery - ${{ inputs.NON_VANILLA == 'true' && 'NON-VANILLA' || 'Vanilla' }} Build
          
          **Type**: ${{ inputs.NON_VANILLA == 'true' && 'Non-Vanilla (Full Features)' || 'Vanilla (Basic)' }}
          **AVB**: ${{ inputs.AVB_DISABLE == 'true' && 'Disabled (Full bypass)' || 'Enabled' }}
          **Manifest**: fox_${{ inputs.MANIFEST_BRANCH }}
          **Device**: ${{ inputs.DEVICE_NAME }}
          **Target**: ${{ inputs.BUILD_TARGET }}
          **Date**: ${{ env.BUILD_DATE }}
          
          **Device Tree**: [${{ inputs.DEVICE_TREE }}](${{ inputs.DEVICE_TREE }}/tree/${{ inputs.DEVICE_TREE_BRANCH }})
          **Commit**: [${{ env.COMMIT_ID }}](${{ inputs.DEVICE_TREE }}/commit/${{ env.COMMIT_ID }})
          
          ${{ inputs.NON_VANILLA == 'true' && '
          ### 🚀 Non-Vanilla Features Enabled:
          - ✅ GSI + Dynamic Partition Support
          - ✅ Full AVB 2.0 Bypass
          - ✅ MagiskBoot Integration
          - ✅ Nano Editor + Bash Shell
          - ✅ All Binary Tools (tar/zip/sed)
          - ✅ FBE + Encryption Bypass' || '' }}
